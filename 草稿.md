# 草稿

```
zhou.xuan,young.lin

zhou.xuan,young.lin



2023-10-30 09:58:00 至 2023-10-30 10:03:00

5分钟内(_cw_app:sale-process-order)中(`_cw_env`:"prod") AND "|ERROR|" and not 未登陆 and not 没有权限 and not supported and not SessionKey非法 and not getWriter and not "java.io.EOFException" not "更新库存失败-必须填写出库仓库" not "AutoPlaceOrderSchedule"not "Broken" not "not supported" not "getWriter()" not "抖店订单签收邀评短信发送失败"的事件总数为84






#uBXgKfN3T


#uBXgKfN3T
```

```
在 ClickHouse 中，配置文件通常位于 /etc/clickhouse-server/config.xml 或者 /usr/local/etc/clickhouse-server/config.xml。如果要启用配置热加载，需要在配置文件中添加以下行：

<remote_servers incl="clickhouse_remote_servers"></remote_servers>
然后，在 ClickHouse 的命令行界面中，可以通过执行以下命令来重新加载配置文件：

SYSTEM RELOAD CONFIG
执行这个命令后，ClickHouse 会重新加载配置文件，并且立即生效。如果配置文件中的修改有误，ClickHouse 会输出相应的错误信息。如果修改正确，则会输出重新加载配置文件的成功信息。
```





```





http://{tengie_ip}:18080/doucAdminWeb/index.html 				
```


